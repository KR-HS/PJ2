<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pieum.capsule.model.CommentMapper">

	<!-- 댓글 추가 -->
	<insert id="insertComment"
		parameterType="pieum.capsule.model.CommentDTO">
		INSERT INTO comments (cno, bno, user_id, content, created_at)
		VALUES (#{cno}, #{bno}, #{userId}, #{content}, NOW())
	</insert>

	<!-- 댓글 상세 조회 -->
	<select id="selectCommentDetail" parameterType="string"
		resultType="pieum.capsule.model.CommentDTO">
		SELECT * FROM comments WHERE cno = #{cno}
	</select>

	<!-- 댓글 수정 -->
	<update id="updateComment"
		parameterType="pieum.capsule.model.CommentDTO">
		UPDATE comments
		SET content = #{content}, updated_at = NOW()
		WHERE cno = #{cno}
	</update>

	<!-- 댓글 삭제 -->
	<delete id="deleteComment" parameterType="string">
		DELETE FROM comments WHERE cno = #{cno}
	</delete>

	<!-- 특정 게시글의 댓글 목록 조회 -->
	<select id="selectCommentList" parameterType="string"
		resultType="pieum.capsule.model.CommentDTO">
		SELECT * FROM comments WHERE bno = #{bno} ORDER BY created_at ASC
	</select>

	<!-- 특정 게시글의 댓글 개수 조회 -->
	<select id="selectCommentTotalCount" parameterType="string"
		resultType="int">
		SELECT COUNT(*) FROM comments WHERE bno = #{bno}
	</select>

</mapper>
