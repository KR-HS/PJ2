<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pieum.user.model.GroupMapper">

    <!-- 그룹 생성 -->
    <insert id="insertGroup" parameterType="pieum.user.model.GroupDTO">
        INSERT INTO groups (gno, name, description, created_at) 
        VALUES (#{gno}, #{name}, #{description}, NOW())
    </insert>

    <!-- 그룹 상세 조회 -->
    <select id="selectGroupDetail" parameterType="string" resultType="pieum.user.model.GroupDTO">
        SELECT * FROM groups WHERE gno = #{gno}
    </select>

    <!-- 그룹 삭제 -->
    <delete id="deleteGroup" parameterType="string">
        DELETE FROM groups WHERE gno = #{gno}
    </delete>

    <!-- 전체 그룹 목록 조회 -->
    <select id="selectGroupList" resultType="pieum.user.model.GroupDTO">
        SELECT * FROM groups ORDER BY created_at DESC
    </select>

    <!-- 특정 사용자가 속한 그룹 목록 조회 -->
    <select id="selectUserGroupList" parameterType="string" resultType="pieum.user.model.GroupDTO">
        SELECT g.* 
        FROM groups g
        JOIN user_groups ug ON g.gno = ug.gno
        WHERE ug.user_id = #{userId}
    </select>

    <!-- 그룹 검색 -->
    <select id="searchGroups" parameterType="string" resultType="pieum.user.model.GroupDTO">
        SELECT * FROM groups WHERE name LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <!-- 그룹 총 개수 조회 -->
    <select id="selectGroupTotalCount" resultType="int">
        SELECT COUNT(*) FROM groups
    </select>

</mapper>
